This tutorial follows the video from BugBytes youtube channel.

UPDATED datastar syntax.

Datastar requires all backend responses to use SSE (Server Side Events).

CRUD operations:
    new movie:
        in index page -> open modal window upon button click
        -> populate modal with new film form
        -> upon successful submit -> display success message in modal + close button
        -> automatically refresh table with indicator to display new movie

Adding django-vite and updating tailwind and daisyui setup.